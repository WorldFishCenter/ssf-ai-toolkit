% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effort.R
\name{effort_fit}
\alias{effort_fit}
\title{Train a custom fishing effort prediction model}
\usage{
effort_fit(
  df,
  label_col = "Activity",
  save_path = NULL,
  trip_col = NULL,
  lat_col = NULL,
  lon_col = NULL,
  time_col = NULL,
  colmap = NULL
)
}
\arguments{
\item{df}{Data frame with GPS tracking data and activity labels}

\item{label_col}{Column name containing activity labels. Expected values:
"Fishing", "Searching" (classified as fishing), "Sailing", "Traveling"
(classified as non-fishing). Default: \code{"Activity"}}

\item{save_path}{Optional path to save the trained model (\code{.joblib} file).
Creates parent directories if needed.}

\item{trip_col}{Column name for trip ID (auto-detected if \code{NULL})}

\item{lat_col}{Column name for latitude (auto-detected if \code{NULL})}

\item{lon_col}{Column name for longitude (auto-detected if \code{NULL})}

\item{time_col}{Column name for timestamp (auto-detected if \code{NULL})}

\item{colmap}{Optional named list for column mapping}
}
\value{
Trained model object (Python EffortClassifier). Can be used directly
or saved via the \code{save_path} parameter.
}
\description{
Trains a machine learning model to predict fishing effort from GPS tracks.
Requires labeled training data with activity classifications (e.g., "Fishing",
"Sailing", "Traveling"). The trained model can be saved and used with
\code{effort_predict()}.
}
\examples{
\dontrun{
# Load labeled training data
training_data <- read.csv("labeled_tracks.csv")

# Train and save model
model <- effort_fit(
  training_data,
  label_col = "Activity",
  save_path = "my_effort_model.joblib"
)

# Later, use the model for predictions
new_tracks <- read.csv("new_tracks.csv")
predictions <- effort_predict(new_tracks, model_path = "my_effort_model.joblib")

# Custom column names
model <- effort_fit(
  training_data,
  label_col = "ActType",
  colmap = list(
    trip_id = "VesselID",
    time = "GPS_Time"
  ),
  save_path = "custom_model.joblib"
)
}
}
