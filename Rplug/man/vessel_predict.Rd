% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vessel.R
\name{vessel_predict}
\alias{vessel_predict}
\title{Predict vessel type from trip-level features}
\usage{
vessel_predict(df, model_path = NULL)
}
\arguments{
\item{df}{Data frame with trip-level features. Must contain columns:
\itemize{
\item \code{duration_hours}: Trip duration in hours
\item \code{distance_nm}: Total distance traveled in nautical miles
\item \code{mean_sog}: Mean speed over ground
}}

\item{model_path}{Path to a trained vessel model file (\code{.joblib}). If \code{NULL},
attempts to load a default model (if available).}
}
\value{
Data frame with original data plus prediction column:
\itemize{
\item \code{vessel_type_pred}: Predicted vessel type (e.g., "motorized", "non-motorized")
}
}
\description{
Predicts vessel type (motorized vs. non-motorized) from aggregated trip-level
features. Requires a trained model file (\code{.joblib}) from prior training via
\code{vessel_fit()}.
}
\examples{
\dontrun{
# Load trip-level data
trips <- read.csv("trip_features.csv")

# Required columns: duration_hours, distance_nm, mean_sog
head(trips[, c("trip_id", "duration_hours", "distance_nm", "mean_sog")])

# Predict vessel type
predictions <- vessel_predict(trips, model_path = "vessel_model.joblib")

# View results
table(predictions$vessel_type_pred)
}
}
