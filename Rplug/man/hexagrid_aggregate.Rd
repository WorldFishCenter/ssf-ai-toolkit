% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hexagrid.R
\name{hexagrid_aggregate}
\alias{hexagrid_aggregate}
\title{Aggregate GPS fishing tracks into H3 hexagonal grid cells}
\usage{
hexagrid_aggregate(
  df,
  resolutions = c(7L, 8L),
  lat_col = NULL,
  lon_col = NULL,
  time_col = NULL,
  trip_col = NULL,
  fishing_col = NULL,
  output_dir = NULL,
  save_parquet = TRUE,
  save_csv = FALSE,
  min_hours = 0.1,
  min_trips = 1L,
  min_days = 1L
)
}
\arguments{
\item{df}{Data frame with GPS fishing track data including effort predictions.
Must contain columns for latitude, longitude, timestamp, and trip ID.
Should also include a fishing classification column (0/1).}

\item{resolutions}{Integer vector of H3 resolutions to compute. Default is
\code{c(7L, 8L)} (regional and detail scales).}

\item{lat_col}{Column name for latitude (auto-detected if \code{NULL})}

\item{lon_col}{Column name for longitude (auto-detected if \code{NULL})}

\item{time_col}{Column name for timestamp (auto-detected if \code{NULL})}

\item{trip_col}{Column name for trip ID (auto-detected if \code{NULL})}

\item{fishing_col}{Column name for fishing classification 0/1
(auto-detected if \code{NULL}, looks for \code{is_fishing}, \code{effort_pred}, etc.)}

\item{output_dir}{Directory path to save output files. If \code{NULL}, results
are only returned in memory.}

\item{save_parquet}{Logical; save results as parquet files (default: \code{TRUE}).
Recommended for large datasets and dashboard use.}

\item{save_csv}{Logical; also save results as CSV files (default: \code{FALSE})}

\item{min_hours}{Minimum fishing hours per hexagon to include (default: \code{0.1})}

\item{min_trips}{Minimum number of trips per hexagon to include (default: \code{1})}

\item{min_days}{Minimum number of days per hexagon to include (default: \code{1})}
}
\value{
Named list with aggregation results per resolution (e.g., \code{res7}, \code{res8}).
Each element contains temporal breakdowns: \code{overall}, \code{year}, \code{season},
\code{month}, \code{daynight}.
}
\description{
Aggregates GPS fishing predictions into H3 hexagonal grid cells with
time-weighted fishing effort metrics. Computes multiple temporal dimensions
(overall, year, season, month, day/night) suitable for dashboard generation
and spatial visualization.

Requires that the input data frame already contains fishing effort predictions
(e.g., from \code{effort_predict_statistical()}).
}
\examples{
\dontrun{
# Step 1: Get fishing effort predictions
tracks <- read.csv("gps_tracks.csv")
predictions <- effort_predict_statistical(tracks)

# Step 2: Aggregate into hexagons
hex_results <- hexagrid_aggregate(predictions)

# Access overall summary for resolution 7
hex_results$res7$overall

# Save results to disk for dashboard use
hex_results <- hexagrid_aggregate(
  predictions,
  resolutions = c(7L, 8L),
  output_dir = "hex_output",
  save_parquet = TRUE
)
}
}
