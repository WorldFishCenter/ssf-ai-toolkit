% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gear.R
\name{gear_predict}
\alias{gear_predict}
\title{Predict fishing gear type from trip-level features}
\usage{
gear_predict(df, model_path = NULL)
}
\arguments{
\item{df}{Data frame with trip-level features. Must contain columns:
\itemize{
\item \code{duration_hours}: Trip duration in hours
\item \code{distance_nm}: Total distance traveled in nautical miles
\item \code{mean_sog}: Mean speed over ground
}}

\item{model_path}{Path to a trained gear model file (\code{.joblib}). If \code{NULL},
attempts to load a default model (if available).}
}
\value{
Data frame with original data plus prediction column:
\itemize{
\item \code{gear_pred}: Predicted gear type (e.g., "gillnet", "handline", "longline")
}
}
\description{
Predicts fishing gear type (e.g., gillnet, handline, longline) from aggregated
trip-level features. Requires a trained model file (\code{.joblib}) from prior
training via \code{gear_fit()}.
}
\examples{
\dontrun{
# Load trip-level data
trips <- read.csv("trip_features.csv")

# Required columns: duration_hours, distance_nm, mean_sog
head(trips[, c("trip_id", "duration_hours", "distance_nm", "mean_sog")])

# Predict gear type
predictions <- gear_predict(trips, model_path = "gear_model.joblib")

# View results
table(predictions$gear_pred)
}
}
