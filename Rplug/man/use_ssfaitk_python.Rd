% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{use_ssfaitk_python}
\alias{use_ssfaitk_python}
\title{Configure Python environment for use with ssfaitk}
\usage{
use_ssfaitk_python(python = NULL, install = FALSE, version = "main")
}
\arguments{
\item{python}{Path to the Python interpreter to use. If \code{NULL} (default),
uses the \code{RETICULATE_PYTHON} environment variable. If neither is set,
reticulate uses its own discovery logic.}

\item{install}{Logical; if \code{TRUE}, installs \code{ssfaitk} if not already present
(default: \code{FALSE}). Pass \code{version} to pin the release.}

\item{version}{Version tag to install if \code{install = TRUE} (default: \code{"main"}).
Use a release tag, e.g. \code{"v0.2.0"}, to pin to a specific version.}
}
\value{
Invisibly returns the configured Python path
}
\description{
Points reticulate to a specific Python interpreter and optionally installs
\code{ssfaitk}. Call this \strong{once at the top of your script or workflow}, before
any other \code{ssfaitk} function. This is especially important in GitHub Actions
and other CI environments where multiple Python installations may exist.

In GitHub Actions, set the \code{RETICULATE_PYTHON} environment variable (via the
\verb{env:} block or \code{Sys.setenv()}) to the Python interpreter where you installed
ssfaitk, and this function will pick it up automatically.
}
\examples{
\dontrun{
# In a GitHub Actions R script — Python path set via RETICULATE_PYTHON env var:
# (in workflow yaml)  env:
#                       RETICULATE_PYTHON: /usr/bin/python3
use_ssfaitk_python()

# Point to a specific interpreter and auto-install if missing
use_ssfaitk_python(python = "/usr/bin/python3", install = TRUE, version = "v0.2.0")

# In a virtualenv workflow
use_ssfaitk_python(python = "/path/to/venv/bin/python")
}
}
