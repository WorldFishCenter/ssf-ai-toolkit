% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effort.R
\name{effort_predict_statistical}
\alias{effort_predict_statistical}
\title{Predict fishing effort using statistical (rule-based) classifier}
\usage{
effort_predict_statistical(
  df,
  filter = FALSE,
  trip_col = NULL,
  lat_col = NULL,
  lon_col = NULL,
  time_col = NULL,
  colmap = NULL,
  config = NULL
)
}
\arguments{
\item{df}{Data frame with GPS tracking data}

\item{filter}{Logical; enable shore distance filtering to remove on-land and
near-shore points. Requires coastline data. Default: \code{FALSE}}

\item{trip_col}{Column name for trip ID (auto-detected if \code{NULL})}

\item{lat_col}{Column name for latitude (auto-detected if \code{NULL})}

\item{lon_col}{Column name for longitude (auto-detected if \code{NULL})}

\item{time_col}{Column name for timestamp (auto-detected if \code{NULL})}

\item{colmap}{Optional named list for column mapping (alternative to individual parameters)}

\item{config}{Optional named list with behavioral thresholds for classification.
See Python documentation for available parameters.}
}
\value{
Data frame with original data plus prediction columns:
\itemize{
\item \code{is_fishing}: Binary prediction (0 or 1)
\item \code{fishing_score}: Continuous fishing likelihood score (0-1)
\item Additional feature columns (speed, acceleration, turning behavior, etc.)
}
}
\description{
Predicts fishing effort using a rule-based classifier that doesn't require training.
Analyzes movement patterns (speed, acceleration, turning behavior) to identify
fishing activity. This is useful when no labeled training data is available.

Includes optional shore distance filtering to remove GPS points that are on land
or too close to shore, which helps prevent false positives from coastal navigation.
}
\examples{
\dontrun{
# Basic usage - no model required!
tracks <- read.csv("gps_tracks.csv")
predictions <- effort_predict_statistical(tracks)

# With shore filtering (removes on-land/near-shore points)
predictions <- effort_predict_statistical(tracks, filter = TRUE)

# View fishing activity summary
table(predictions$is_fishing)

# With custom column mapping
predictions <- effort_predict_statistical(
  tracks,
  filter = TRUE,
  colmap = list(
    trip_id = "VesselID",
    time = "GPS_DateTime",
    lat = "Lat",
    lon = "Lon"
  )
)
}
}
