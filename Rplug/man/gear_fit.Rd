% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gear.R
\name{gear_fit}
\alias{gear_fit}
\title{Train a custom fishing gear prediction model}
\usage{
gear_fit(df, label_col = "gear_label", save_path = NULL)
}
\arguments{
\item{df}{Data frame with trip-level features and gear labels. Must contain:
\itemize{
\item \code{duration_hours}: Trip duration in hours
\item \code{distance_nm}: Total distance in nautical miles
\item \code{mean_sog}: Mean speed over ground
\item Gear label column (specified by \code{label_col})
}}

\item{label_col}{Column name containing gear type labels (e.g., "gillnet",
"handline", "longline"). Default: \code{"gear_label"}}

\item{save_path}{Optional path to save the trained model (\code{.joblib} file).
Creates parent directories if needed.}
}
\value{
Trained model object (Python GearPredictor). Can be used directly
or saved via the \code{save_path} parameter.
}
\description{
Trains a machine learning model to predict fishing gear type from trip-level
features. Requires labeled training data with gear type classifications.
The trained model can be saved and used with \code{gear_predict()}.
}
\examples{
\dontrun{
# Load labeled training data
training_trips <- data.frame(
  trip_id = 1:100,
  duration_hours = runif(100, 2, 8),
  distance_nm = runif(100, 5, 20),
  mean_sog = runif(100, 1.5, 4.0),
  gear_label = sample(c("gillnet", "handline", "longline"), 100, replace = TRUE)
)

# Train and save model
model <- gear_fit(
  training_trips,
  label_col = "gear_label",
  save_path = "my_gear_model.joblib"
)

# Later, use the model for predictions
new_trips <- read.csv("new_trips.csv")
predictions <- gear_predict(new_trips, model_path = "my_gear_model.joblib")
}
}
