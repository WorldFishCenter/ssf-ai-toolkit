% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effort.R
\name{effort_predict}
\alias{effort_predict}
\title{Predict fishing effort using a trained model}
\usage{
effort_predict(df, model_path = NULL, return_proba = TRUE, colmap = NULL)
}
\arguments{
\item{df}{Data frame with GPS tracking data. Must contain columns for trip ID,
timestamp, latitude, and longitude. Column names are auto-detected but can be
customized using \code{colmap}.}

\item{model_path}{Path to a trained model file (\code{.joblib}). If \code{NULL}, attempts
to load a default model (if available).}

\item{return_proba}{Logical; whether to return probability scores (default: \code{TRUE})}

\item{colmap}{Optional named list mapping standard column names to your actual
column names. Valid keys: \code{trip_id}, \code{time}/\code{timestamp}, \code{lat}/\code{latitude},
\code{lon}/\code{longitude}. Example: \code{list(trip_id = "VesselID", time = "GPS_Time")}}
}
\value{
Data frame with original data plus prediction columns:
\itemize{
\item \code{effort_pred}: Binary prediction (0 = non-fishing, 1 = fishing)
\item \code{effort_prob}: Fishing probability (0-1), if \code{return_proba = TRUE}
}
}
\description{
Predicts fishing effort on GPS tracks using a machine learning model.
Requires a trained model file (\code{.joblib}) from prior training via \code{effort_fit()}.
}
\examples{
\dontrun{
# Basic usage
tracks <- read.csv("gps_tracks.csv")
predictions <- effort_predict(tracks, model_path = "effort_model.joblib")

# With custom column mapping
predictions <- effort_predict(
  tracks,
  model_path = "effort_model.joblib",
  colmap = list(
    trip_id = "VesselID",
    time = "GPS_DateTime",
    lat = "Lat_DD",
    lon = "Lon_DD"
  )
)

# View results
table(predictions$effort_pred)
}
}
